import GetToken from "@/features/token/GetToken";
import React from "react";

function token() {
  return (
    <main className="flex justify-center items-center mt-20">
     <GetToken/>
    </main>
  );
}

export default token;
